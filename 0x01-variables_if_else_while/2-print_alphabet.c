#include <stdio.h>
/* main checks for alpha character a to z
 * 
 * returns 1 when printing a to z 
 */
int main(void)
char (alpha=a)
{
for(char alpha = 'a'; alpha <=z ; alpha++)
{
	putchar(alpha);
}
putchar('\n');
return (0);
}
