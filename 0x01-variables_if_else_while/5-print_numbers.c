#include <stdio.h>
/**
* main - checks for alpha character a to Z
*
* Return: 1 when printing a to Z
*/
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
return (0);
}
